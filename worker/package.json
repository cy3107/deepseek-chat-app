{
  "name": "deepseek-chat-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for DeepSeek chat app",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^18.15.11",
    "typescript": "^4.9.5",
    "wrangler": "^3.0.0"
  }
}